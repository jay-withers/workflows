# Internal: this repo's own use of the reusable release.yml workflow.
# Not part of the public API — other repos should call release.yml directly.
name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
    with:
      update-major-tag: true
